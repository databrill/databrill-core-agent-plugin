{
	"lock": false,
	"tasks": {
		"check": "deno check --quiet",
		"ci": "deno task static:ci && deno task test",
		"fmt:check": "deno fmt --check",
		"fmt": "deno fmt",
		"lint": "deno lint",
		"static:ci": "deno --quiet task fmt:check && deno --quiet task check && deno --quiet task lint",
		"static": "deno --quiet fmt && deno --quiet task check && deno --quiet task lint",
		"test": "deno test scripts/query_test.ts"
	},
	"fmt": {
		"useTabs": true,
		"lineWidth": 120,
		"indentWidth": 4,
		"semiColons": true,
		"singleQuote": false,
		"proseWrap": "preserve",
		"exclude": [
			"docs/schema"
		]
	},
	"lint": {
		"rules": {
			"tags": [
				"recommended"
			]
		}
	}
}
